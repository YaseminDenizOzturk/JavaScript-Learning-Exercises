let result;

let x = 25, y = 40, z = 90;


result = x + y;
result = x - y;
result = x * y;
result = x / y;
result = z % x;

result = x++;
result = ++x;
result = --x;
result = x--;

result = x;
result += x; // result = result + x

// result ile x arasındaki işlemi gerçekleştir.

result += x;
result -= x;
result *= x;
result /= x;
result %= x;

result *= (x + y);


console.log(result);
console.log(x);

result = (x == y) // eşitse true 
result = (x != y) // eşitse false
result = (5 === "5") // üç eşittir javascriptte iki eşittire ek olarak tip kontrolü de yapar.
// mesela bu örnek false çıktısı verir .  number ile string birbirine eşit değildir.

result = (x > y) // x, y den küçüktür (false) 








